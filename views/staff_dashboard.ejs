<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Staff Dashboard</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f9;
    }

    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: #0073e6;
      color: white;
      padding: 10px 20px;
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 1000;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .navbar h1 {
      margin: 0;
      font-size: 24px;
      font-weight: bold;
    }

    .nav-options {
      display: flex;
      align-items: center;
      gap: 20px;
    }

    .nav-options a {
      color: white;
      text-decoration: none;
      padding: 10px;
      border-radius: 5px;
      transition: background-color 0.3s;
      position: relative;
    }

    .nav-options a:hover {
      background-color: #005bb5;
    }

    .dropdown-menu a {
      color: #333;
      text-decoration: none;
      padding: 10px 15px;
      display: block;
      transition: background-color 0.3s;
    }

    .dropdown-menu a:hover {
      background-color: #f1f1f1;
    }

    .dashboard {
      display: flex;
      flex-direction: column;
      gap: 20px;
      margin: 20px auto;
      padding: 20px;
      max-width: 1200px;
    }

    .dashboard-section {
      background: white;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .dashboard-section:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
    }

    .dashboard-section h2 {
      margin-top: 0;
      color: #0073e6;
      font-size: 22px;
      font-weight: bold;
    }

    .dashboard-section p {
      color: #555;
      margin-bottom: 20px;
      font-size: 16px;
    }

    .dashboard-section button {
      background-color: #0073e6;
      color: white;
      border: none;
      padding: 12px 20px;
      border-radius: 8px;
      cursor: pointer;
      font-size: 16px;
      font-weight: bold;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    .dashboard-section button:hover {
      background-color: #005bb5;
      transform: scale(1.05);
    }

    .dashboard-section button:active {
      background-color: #003f7f;
      transform: scale(0.95);
    }
  </style>
</head>
<body>
  <!-- Navigation Bar -->
  <div class="navbar">
    <h1>Langley Eats</h1>
    <div class="nav-options">
      <a href="/view-orders" data-tooltip="View Orders"><i class="fas fa-list"></i></a>
      <a href="/menu_update" data-tooltip="Update Menu"><i class="fas fa-edit"></i></a>
      <a href="/studentstaff" data-tooltip="Update Collection Times"><i class="fas fa-clock"></i></a>
      <!-- Account Options Dropdown -->
      <div class="dropdown">
        <a class="dropdown-toggle" href="#" role="button" id="accountDropdown" data-bs-toggle="dropdown" aria-expanded="false">
          <i class="fas fa-user"></i>
        </a>
        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="accountDropdown">
          <li><a class="dropdown-item" href="/profile">Account Details</a></li>
          <li><a class="dropdown-item" href="/logout">Logout</a></li>
        </ul>
      </div>
    </div>
  </div>

  <!-- Dashboard Sections -->
  <div class="dashboard" style="margin-top: 80px;">
    <div class="dashboard-section">
      <h2><i class="fas fa-list"></i> View Orders</h2>
      <p>Access the list of current orders to prepare and fulfill them efficiently.</p>
      <button onclick="window.location.href='/view-orders'">View Orders</button>
    </div>

    <div class="dashboard-section">
      <h2><i class="fas fa-edit"></i> Update Menu</h2>
      <p>Make changes to the menu, including adding, editing, or removing items.</p>
      <button onclick="window.location.href='/menu_update'">Update Menu</button>
    </div>

    <div class="dashboard-section">
      <h2><i class="fas fa-clock"></i> Update Collection Times</h2>
      <p>Modify the collection times for orders to ensure timely pickups.</p>
      <button onclick="window.location.href='/studentstaff'">Update Collection Times</button>
    </div>
  </div>
</body>
</html>