<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Student Dashboard</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f9;
    }

    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: #0073e6;
      color: white;
      padding: 10px 20px;
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 1000;
    }

    .navbar h1 {
      margin: 0;
    }

    .nav-options {
      display: flex;
      align-items: center;
      gap: 20px;
    }

    .nav-options a {
      color: white;
      text-decoration: none;
      padding: 10px;
      border-radius: 5px;
      transition: background-color 0.3s;
      position: relative;
    }

    .nav-options a:hover {
      background-color: #005bb5;
    }

    .nav-options a::after {
      content: attr(data-tooltip);
      position: absolute;
      bottom: -25px;
      left: 50%;
      transform: translateX(-50%);
      background-color: #333;
      color: white;
      padding: 5px;
      border-radius: 5px;
      opacity: 0;
      transition: opacity 0.3s;
      pointer-events: none;
      white-space: nowrap;
    }

    .nav-options a:hover::after {
      opacity: 1;
    }

    .profile-dropdown {
      position: relative;
    }

    .profile-dropdown-content {
      display: none;
      position: absolute;
      right: 0;
      background-color: white;
      color: black;
      min-width: 150px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
      z-index: 1;
      border-radius: 5px;
      overflow: hidden;
    }

    .profile-dropdown-content a {
      color: black;
      padding: 12px 16px;
      text-decoration: none;
      display: block;
      transition: background-color 0.3s;
    }

    .profile-dropdown-content a:hover {
      background-color: #f1f1f1;
    }

    .profile-dropdown:hover .profile-dropdown-content {
      display: block;
    }

    .container {
      display: flex;
      padding: 20px;
      height: calc(100vh - 60px);
      margin-top: 60px;
    }

    .advertisement {
      flex: 1;
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      padding: 20px;
      margin-right: 20px;
    }

    .advertisement img {
      width: 100%;
      height: auto;
      border-radius: 8px;
    }

    .right-section {
      display: flex;
      flex-direction: column;
      flex: 2;
    }

    .carousel-container {
      flex: 1;
      margin-bottom: 20px;
    }

    .order-now-container {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 80px;
      width: 100%;
    }

    .order-now {
      background-color: #0073e6;
      color: white;
      border: 2px solid #0073e6;
      border-radius: 8px;
      padding: 15px 30px;
      text-align: center;
      font-size: 20px;
      cursor: pointer;
      transition: background-color 0.3s ease, border-color 0.3s ease;
      text-decoration: none;
      display: block;
      width:100%;
      text-align: center;
    }

    .order-now:hover {
      background-color: #005bb5;
      border-color: #005bb5;
    }

    .carousel {
      height: 100%;
    }

    .carousel-inner {
      height: 100%;
    }

    .carousel-image {
      object-fit: contain;
      height: 100%;
      width:100%;
      border-radius: 8px;
    }
  </style>
</head>
<body>
  <!-- Navigation Bar -->
  <div class="navbar">
    <h1>Langley Eats</h1>
    <div class="nav-options">
      <a href="/menu" data-tooltip="Menu"><i class="fas fa-utensils"></i></a>
      <a href="/order_status" data-tooltip="Order Status"><i class="fas fa-receipt"></i></a>
      <div class="profile-dropdown">
        <a href="#" data-tooltip="Profile"><i class="fas fa-user"></i></a>
        <div class="profile-dropdown-content">
          <a href="/profile">Account Details</a>
          <a href="/logout">Logout</a>
        </div>
      </div>
    </div>
  </div>

  <!-- Main Content -->
  <div class="container">
    <!-- Advertisement Section -->
    <div class="advertisement">
      <img src="/uploads/Burger.jpeg" alt="poster">
    </div>

    <!-- Right Section -->
    <div class="right-section">
      <div class="carousel-container">
        <div id="demo" class="carousel slide h-100" data-bs-ride="carousel">
          <div class="carousel-indicators">
            <button type="button" data-bs-target="#demo" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
            <button type="button" data-bs-target="#demo" data-bs-slide-to="1" aria-label="Slide 2"></button>
            <button type="button" data-bs-target="#demo" data-bs-slide-to="2" aria-label="Slide 3"></button>
          </div>

          <!-- The carousel -->
          <div class="carousel-inner h-100">
            <div class="carousel-item active">
              <img src="/public/uploads/food.jpg" alt="Food" class="d-block w-100 h-100 carousel-image">
            </div>
            <div class="carousel-item">
              <img src="/public/uploads/Burger.jpeg" alt="Burger" class="d-block w-100 h-100 carousel-image">
            </div>
            <div class="carousel-item">
              <img src="/public/uploads/Poster.jpg" alt="Poster" class="d-block w-100 h-100 carousel-image">
            </div>
          </div>

          <!-- Left and right controls/icons -->
          <button class="carousel-control-prev" type="button" data-bs-target="#demo" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
          </button>
          <button class="carousel-control-next" type="button" data-bs-target="#demo" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
          </button>
        </div>
      </div>

      <!-- Order Now Button -->
      <div class="order-now-container">
        <a href="/menu" class="order-now">
          Order Now
        </a>
      </div>
    </div>
  </div>
</body>
</html>